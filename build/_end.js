  // Ask almond to synchronously require the
  // module value for 'jetKomponents' here and return it as the
  // value to use for the public API for the built file.
  return require('jetKomponents');
}));
